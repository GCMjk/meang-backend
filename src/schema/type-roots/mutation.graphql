type Mutation {
    register(user: UserInput!): ResultUser
    updateUser(user: UserInput!): ResultUser
    deleteUser(id: ID!): ResultUser
    addCategory(category: String!): ResultCategory
    updateCategory(id: ID!, category: String!): ResultCategory
    deleteCategory(id: ID!): ResultCategory
}